/* eslint-disable */

import { showLoading } from '../utils/showLoading.js';

describe('Clase showLoading', () => {
    test('showLoading', () => {
        showLoading(true);
        showLoading(false);
    });
});